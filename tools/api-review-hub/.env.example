ENVIRONMENT_NAME=staging

# Optional: grants your signed-in user developer access to the created resources.
# Get this with: az ad signed-in-user show --query id -o tsv
ASSIGNEE_OBJECT_ID=

# Optional: use a non-default variables file path.
# VARIABLES_PATH=scripts/infra/variables.yaml