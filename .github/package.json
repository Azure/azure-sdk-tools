{
  "name": "azure-sdk-tools-github",
  "private": true,
  "scripts": {
    "format:check": "prettier --check \"{aw,skills}/**/*.{md,yaml,yml,json}\"",
    "format": "prettier --write \"{aw,skills}/**/*.{md,yaml,yml,json}\""
  },
  "devDependencies": {
    "prettier": "3.8.4"
  }
}