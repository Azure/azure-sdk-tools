namespace APIViewWeb.Repositories
{
    public interface ICosmospenAIRepository
    {
        // redo wording 
        // cosmos openai repo 

        // controller -> get create request 
        // 

        // logic of creating embedding -> openai comments manager

        // mongo db = repo
        // openai = repo 

        // one class for mongo, one for openai, one for business logic (API logic) openaimaager
        // reviewmaager: get the request 
        // 
    }
}
