trigger:
  branches:
    include:
      - main
  paths:
    include:
      - tools/stress-cluster
      - eng/common/scripts/stress-testing

pr:
  branches:
    include:
      - main
  paths:
    include:
      - tools/stress-cluster
      - eng/common/scripts/stress-testing

jobs:
  - job: TestAppService
    pool:
      name: azsdk-pool-mms-ubuntu-2204-general
      vmImage: ubuntu-22.04
    steps:
      - pwsh: |
          Write-Host "Testing app service"
          curl https://myPingServer13328.azurewebsites.net/ping
          echo "EXIT: $LASTEXITCODE"
