{
  "type": "json_schema",
  "json_schema": {
    "name": "report_issue_result",
    "strict": true,
    "schema": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "A concise GitHub issue title starting with [APIView]."
        },
        "body": {
          "type": "string",
          "description": "Technical context the LLM can add beyond what the user reported — likely causes, related components, guidelines references, or investigation suggestions. Empty string if nothing useful to add."
        }
      },
      "required": ["title", "body"],
      "additionalProperties": false
    }
  }
}
