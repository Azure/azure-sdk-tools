# Refer mirror-repos.yml.

trigger: none
pr: none

pool:
  vmImage: ubuntu-latest

jobs:
  - job: SyncRepos
    displayName: Sync source repos to target repos

    steps:
    - template: /eng/pipelines/templates/steps/sync-repo.yml
      parameters:
        Repos:
          Jacktn/TestRepo-one:
            TargetRepos:
              Jacktn/TestRepo-two:
