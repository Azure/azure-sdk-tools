name: sensei-evals
description: Evaluations for the sensei skill compliance improver
skill: sensei
config:
  timeout: 60
graders:
- type: regex
  name: skill-activated
  config:
    must_match:
    - "(?i)skill"
tasks: tasks/*.yaml
