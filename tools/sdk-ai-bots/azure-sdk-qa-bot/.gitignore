# Generated by azd's postprovision hook (deployment/hooks/lib/sync-teams-env.ts)
# from the current azd environment. Regenerated on every `azd provision`, so it
# is machine/environment-specific and must not be committed.
env/.env.azd
env/.env.azd.user
