name: markdown-token-optimizer-eval
description: Evaluation suite for markdown token optimizer skill.
skill: markdown-token-optimizer
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 300
  parallel: false
graders:
  - type: text
    name: mentions_optimization_concepts
    config:
      contains: ["token", "markdown", "optimize", "reduce", "compression"]
  - type: code
    name: has_substantive_output
    config:
      assertions:
        - "len(output) > 50"
tasks:
  - "tasks/*.yaml"
