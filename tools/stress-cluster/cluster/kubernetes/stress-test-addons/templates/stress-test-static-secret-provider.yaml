apiVersion: secrets-store.csi.x-k8s.io/v1alpha1
kind: SecretProviderClass
metadata:
  name: stress-static-kv-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  provider: azure
  secretObjects:
    - secretName: {{ .Values.subscription }}
      type: Opaque
      data:
        - objectName: {{ .Values.subscription }}
          key: value
  parameters:
    useVMManagedIdentity: "true"
    userAssignedIdentityID: {{ .Values.secretProviderIdentity }}  # az vmss identity show ...
    keyvaultName: {{ .Values.staticTestSecretsKeyvaultName }}
    objects:  |
      array:
        - |
          objectName: {{ .Values.subscription }}
          objectType: secret
    tenantId: {{ .Values.tenantId }}
