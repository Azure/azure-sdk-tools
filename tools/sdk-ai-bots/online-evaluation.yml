schedules:
  - cron: "0 1 * * 1"  # Runs every Monday at 01:00 UTC
    displayName: "Weekly Monday Morning Trigger"
    branches:
      include:
        - main
    always: true

trigger: none
pr: none

# Online (weekly, production) evaluation of the deployed bot.
#
# Evaluates via completion mode against the deployed bot's $(BOT_SERVICE_ENDPOINT)
# /completion endpoint (concurrent collection + inline grading) on the curated basic
# datasets. Results land on the Evaluation tab in the Azure AI Foundry portal.
extends:
  template: /eng/pipelines/templates/stages/1es-redirect.yml
  parameters:
    Use1ESOfficial: false
    stages:
      - stage: 'evaluation'
        variables:
          - template: /eng/pipelines/templates/variables/globals.yml
          - template: /eng/pipelines/templates/variables/image.yml
        pool:
              name: $(LINUXPOOL)
              image: $(LINUXVMIMAGE)
              os: linux
        jobs:
          - job: 'evaluation'
            displayName: 'online evaluation'
            strategy:
              maxParallel: 2
              matrix:
                typespec:
                  scenario: typespec
                onboarding:
                  scenario: onboarding
                python:
                  scenario: python
                apispec:
                  scenario: apispec
            steps:
              - task: UsePythonVersion@0
                    displayName: "Use Python 3.12"
                    inputs:
                      versionSpec: '3.12'
              - script: |
                  pip install -r requirements.txt
                displayName: 'install requirements'
                workingDirectory: $(Build.SourcesDirectory)/tools/sdk-ai-bots/azure-sdk-qa-bot-evaluation
              - task: AzureCLI@2
                displayName: 'Run Evals ($(scenario))'
                inputs:
                  azureSubscription: 'azuresdkqabot'
                  scriptType: bash
                  scriptLocation: inlineScript
                  workingDirectory: $(Build.SourcesDirectory)/tools/sdk-ai-bots/azure-sdk-qa-bot-evaluation
                  inlineScript: |
                    export BOT_AGENT_ACCESS_TOKEN=$(az account get-access-token --resource api://azure-sdk-qa-bot --query accessToken -o tsv)
                    current_date=$(date +%Y_%m_%d)
                    python evals_run.py \
                      --dataset "qa-bot-basic-$(scenario):latest" \
                      --evaluation_name_prefix "online-evaluation-$current_date" \
                      --evaluators "bot_evals" \
                      --max_concurrency 8 \
                      --baseline_check False
                env:
                  SYSTEM_ACCESSTOKEN: $(System.AccessToken)
                  AZURE_AI_PROJECT_ENDPOINT: $(AZURE_AI_PROJECT_ENDPOINT)
                  AZURE_SUBSCRIPTION_ID: $(AZURE_SUBSCRIPTION_ID)
                  AZURE_EVALUATION_MODEL_NAME: $(AZURE_EVALUATION_MODEL_NAME)
                  BOT_SERVICE_ENDPOINT: $(BOT_SERVICE_ENDPOINT)
                  STORAGE_BLOB_ACCOUNT: $(STORAGE_BLOB_ACCOUNT)
                  BOT_CONFIG_CONTAINER: $(BOT_CONFIG_CONTAINER)
                  BOT_CONFIG_CHANNEL_BLOB: $(BOT_CONFIG_CHANNEL_BLOB)
                  EVALUATE_THRESHOLD: $(EVALUATE_THRESHOLD)
