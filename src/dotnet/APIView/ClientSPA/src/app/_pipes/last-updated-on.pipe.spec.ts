import { LastUpdatedOnPipe } from './last-updated-on.pipe';

describe('LastUpdatedOnPipe', () => {
  it('create an instance', () => {
    const pipe = new LastUpdatedOnPipe();
    expect(pipe).toBeTruthy();
  });
});
