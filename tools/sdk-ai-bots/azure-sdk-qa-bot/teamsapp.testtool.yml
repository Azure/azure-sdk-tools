# yaml-language-server: $schema=https://aka.ms/teams-toolkit/v1.7/yaml.schema.json
# Visit https://aka.ms/teamsfx-v5.0-guide for details on this file
# Visit https://aka.ms/teamsfx-actions for details on actions
version: v1.7

deploy:
  # Install development tool(s)
  - uses: devTool/install
    with:
      testTool:
        version: ~0.2.1
        symlinkDir: ./devTools/teamsapptester

  # Run npm command
  - uses: cli/runNpmCommand
    with:
      args: install --no-audit

  # Generate runtime environment variables
  - uses: file/createOrUpdateEnvironmentFile
    with:
      target: ./.localConfigs.testTool
      envs:
        TEAMSFX_NOTIFICATION_STORE_FILENAME: ${{TEAMSFX_NOTIFICATION_STORE_FILENAME}}
        # RAG
        RAG_API_KEY: ${{SECRET_RAG_API_KEY}}
        RAG_ENDPOINT: ${{RAG_ENDPOINT}}
        RAG_TANENT_ID: ${{RAG_TANENT_ID}}
        # Computer vision
        AZURE_COMPUTER_VISION_ENDPOINT: ${{AZURE_COMPUTER_VISION_ENDPOINT}}
        AZURE_COMPUTER_VISION_API_KEY: ${{SECRET_AZURE_COMPUTER_VISION_API_KEY}}
        # Python channel
        CHANNEL_ID_FOR_PYTHON: ${{CHANNEL_ID_FOR_PYTHON}}
        CHANNEL_ID_FOR_PYTHON_DEV_INTERNAL: ${{CHANNEL_ID_FOR_PYTHON_DEV_INTERNAL}}
        RAG_TANENT_ID_FOR_PYTHON: ${{RAG_TANENT_ID_FOR_PYTHON}}
        # Test in local
        IS_LOCAL: true
        # Azure Table
        AZURE_STORAGE_URL: ${{AZURE_STORAGE_URL}}
        AZURE_TABLE_NAME_FOR_CONVERSATION: ${{AZURE_TABLE_NAME_FOR_CONVERSATION}}

