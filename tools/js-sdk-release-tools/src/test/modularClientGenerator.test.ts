import { expect, test } from "vitest";
import { ModularClientGenerator } from "../mlc/clientGenerator/ModularClientGenerator";


test("HLC -> Modular: Rename", async () => {
    const genertor = new ModularClientGenerator();

    expect().toBe(
        
    );
});
