name: Service Tag Check

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl ping endpoint
        run: |
          curl -f https://pingservicetest.azurewebsites.net

