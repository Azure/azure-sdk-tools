{
  "name": "azure-sdk-tools-skill-eval",
  "private": true,
  "description": "CI-only npm project that pins @microsoft/vally-cli and its transitive dependencies via the committed lockfile for reproducible skill-eval CI runs; not published.",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=11.11.1"
  },
  "devDependencies": {
    "@microsoft/vally-cli": "0.12.0"
  }
}
