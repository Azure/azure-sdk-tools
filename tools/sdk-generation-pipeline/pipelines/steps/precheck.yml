parameters:
  - name: sdkPR
    type: object
steps:
  - bash: |
      echo ${#{ parameters.sdkPR.prLink }}
      if [ 2 -lt ${#{ parameters.sdkPR.prLink }} ];then
        echo "2 -lt ${{ parameters.sdkPR.prLink }}"
      else
        echo "2 >= ${{ parameters.sdkPR.prLink }}"
      if
    displayName: Check parameters

