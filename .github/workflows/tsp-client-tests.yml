name: tsp-client - Test

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/tsp-client-tests.yml
      - tools/tsp-client/**
  pull_request:
    paths:
      - .github/workflows/tsp-client-tests.yml
      - tools/tsp-client/**

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl ping endpoint
        run: |
          echo "curl app service"
          curl -f https://pingservicetest.azurewebsites.net
