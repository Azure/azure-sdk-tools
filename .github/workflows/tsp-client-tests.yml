name: tsp-client - Test

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/tsp-client-tests.yml
      - tools/tsp-client/**
  pull_request:
    paths:
      - .github/workflows/tsp-client-tests.yml
      - tools/tsp-client/**

jobs:
  ping:
    runs-on: [self-hosted, 1ES.Pool=azsdk-github-pool]
    steps:
      - name: Curl ping endpoint
        run: |
          echo "curl app service 4"
          curl -f https://pingservicetest.azurewebsites.net
