apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-loss-example
  namespace: default
spec:
  action: loss
  direction: to
  externalTargets:
    - bing.com
  mode: one
  selector:
    labelSelectors:
      testName: foo
      chaos: "true"
    namespaces:
      - default
  loss:
    loss: "100"
    correlation: "0"
