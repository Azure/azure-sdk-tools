name: sensei-eval
description: Evaluation suite for the sensei skill
type: capability

environment: azsdk-mcp-mock

tags:
  area: sensei
  priority: p0

config:
  runs: 1
  timeout: "5m"
  model: claude-opus-4.6
  executor: copilot-sdk

stimuli:
  - name: sensei-basic-001
    prompt: |
      Review this skill frontmatter for compliance:

      ---
      name: example-skill
      description: A skill for doing things
      ---

      Check if it meets vally requirements and suggest improvements.
    graders:
      - type: output-contains
        config:
          substring: "missing fields"
      - type: output-contains
        config:
          substring: "improvements"
      - type: output-contains
        config:
          substring: "requirements"

  - name: basic-trigger
    prompt: "Run sensei on the api-validation skill to improve its frontmatter compliance"
    graders:
      - type: output-contains
        config:
          substring: "skill"
    constraints:
      expect_skills: ["sensei"]

  - name: anti-trigger
    prompt: "How do I set up automatic retries for my HTTP client?"
    graders:
      - type: output-not-contains
        config:
          substring: "frontmatter"
      - type: output-not-contains
        config:
          substring: "Ralph loop"
