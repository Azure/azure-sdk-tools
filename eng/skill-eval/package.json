{
  "name": "azure-sdk-tools-skill-eval",
  "private": true,
  "description": "CI-only npm project that pins @microsoft/vally-cli for the skill-eval pipeline. Restored from the DevOps npm feed via npm ci; not published.",
  "devDependencies": {
    "@microsoft/vally-cli": "0.5.0"
  }
}
