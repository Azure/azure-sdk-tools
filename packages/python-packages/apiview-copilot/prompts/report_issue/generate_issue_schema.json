{
  "type": "json_schema",
  "json_schema": {
    "name": "report_issue_result",
    "strict": true,
    "schema": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "A concise GitHub issue title that begins with the title_prefix the caller passed in (e.g. [APIView], [AVC], or [Python APIView])."
        },
        "body": {
          "type": "string",
          "description": "The full GitHub issue body in Markdown, with the sections specified in the system prompt (Summary / Context / Details / Suggested Next Steps), ending with '---\\n*Reported via APIView*'."
        }
      },
      "required": ["title", "body"],
      "additionalProperties": false
    }
  }
}
