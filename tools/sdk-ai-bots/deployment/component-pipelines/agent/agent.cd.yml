# CD pipeline — agent (hosted Foundry agent).
# Image-only deploy. Infrastructure is provisioned separately by the agent
# provision pipeline or a provision-all-<env> orchestrator.
#
# The agent-server (FastAPI slot) is a separate service deployed by
# component-pipelines/agent-server/agent-server.cd.yml.
name: tools - sdk-ai-bots-agent - cd

trigger: none
pr: none

parameters:
    - name: environment
      type: string
      default: dev
      values: [dev, preview, prod]
    - name: imageTag
      type: string

variables:
    - template: /eng/pipelines/templates/variables/globals.yml
    - template: /eng/pipelines/templates/variables/image.yml

extends:
    template: /eng/pipelines/templates/stages/1es-redirect.yml
    parameters:
        Use1ESOfficial: true
        stages:
            - stage: DeployAgent
              displayName: 'Deploy agent → ${{ parameters.environment }}'
              jobs:
                  - job: Deploy
                    pool:
                        name: $(LINUXPOOL)
                        image: $(LINUXVMIMAGE)
                        os: linux
                    steps:
                        - checkout: self
                        - template: /tools/sdk-ai-bots/deployment/pipelines/templates/load-environment-suite.yml
                          parameters:
                              environment: '${{ parameters.environment }}'
                        - template: /tools/sdk-ai-bots/deployment/pipelines/templates/azd-auth.yml

                        - template: /tools/sdk-ai-bots/deployment/pipelines/templates/azd-deploy.yml
                          parameters:
                              service: agent
                              imageTag: '${{ parameters.imageTag }}'

                        - task: AzureCLI@2
                          displayName: 'Record LastKnownGoodTag'
                          inputs:
                              azureSubscription: $(SERVICE_CONNECTION)
                              scriptType: bash
                              scriptLocation: inlineScript
                              inlineScript: |
                                  az appconfig kv set --name "$APP_CONFIG_NAME" \
                                    --key "Deployment:agent:LastKnownGoodTag" \
                                    --value "${{ parameters.imageTag }}" --yes
