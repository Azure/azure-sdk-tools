steps:
- pwsh:
    git ls-files
    # git ls-files | Group-Object | Where-Object { $_.Count -gt 1 } | Select-Object Group
    # $differByCaseFiles = git ls-files | Group-Object | Where-Object { $_.Count -gt 1 } | Select-Object Group
    # Write-Host $differByCaseFiles

    # if ($differByCaseFiles) 
    # { 
    #   Write-Host "I am here"
    #   # Write-Warning "Here are the files which only differ in case. Please double check and rename the files."
    #   # $differByCaseFiles 
    #   # exit 1
    # }
    # else
    # {
      
    #   Write-Host "I am there"
    # }

    # Write-Host "There are no files only differ in case. "
  displayName: Validate on files only differ on cases
  workingDirectory: $(System.DefaultWorkingDirectory)