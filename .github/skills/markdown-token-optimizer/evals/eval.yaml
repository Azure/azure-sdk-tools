name: markdown-token-optimizer-evals
description: Evaluations for the markdown token optimizer skill
skill: markdown-token-optimizer
config:
  timeout: 60
graders:
- type: regex
  name: skill-activated
  config:
    must_match:
    - "(?i)token"
tasks: tasks/*.yaml
