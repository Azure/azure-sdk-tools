# Orchestrator pipeline — full prod provision.
#
# Two stages, each gated by the `sdk-ai-bots-prod` ADO environment approval:
#   1. PreflightProd — azd preview + readiness reminder.
#   2. ProvisionProd — applies only after preflight succeeds and a second approval.

name: tools - sdk-ai-bots - provision-all-prod

trigger: none
pr: none

variables:
    - template: /eng/pipelines/templates/variables/globals.yml
    - template: /eng/pipelines/templates/variables/image.yml

extends:
    template: /eng/pipelines/templates/stages/1es-redirect.yml
    parameters:
        Use1ESOfficial: true
        stages:
            - template: /tools/sdk-ai-bots/deployment/pipelines/templates/provision-stage.yml
              parameters:
                  environment: prod
                  stageName: PreflightProd
                  stageDisplayName: 'Preflight (preview + readiness)'
                  runValidateAndPreview: true
                  runProvision: false
                  showReadinessReminder: true
            - template: /tools/sdk-ai-bots/deployment/pipelines/templates/provision-stage.yml
              parameters:
                  environment: prod
                  stageName: ProvisionProd
                  stageDisplayName: 'Provision (apply)'
                  runValidateAndPreview: false
                  runProvision: true
