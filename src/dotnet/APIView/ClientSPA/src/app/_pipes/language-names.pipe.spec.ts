import { LanguageNamesPipe } from './language-names.pipe';

describe('LanguageNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
