export enum Environment {
    Development = 'development',
    Production = 'production',
    Test = 'test'
}

export enum SpecRetrievalAuthMode {
    None = 'none',
    Token = 'token'
}

export enum SpecRetrievalMethod {
    Filesystem = 'filesystem',
    Git = 'git'
}
