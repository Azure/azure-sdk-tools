trigger: none

variables:
  - template: ../../eng/pipelines/templates/variables/globals.yml

stages:
  - stage: SdkGeneration
    jobs:
      - job: SdkGeneration
        displayName: test
        steps:
          - template: ./pipelines/steps/precheck.yml

          - template: ./pipelines/steps/precheck2.yml
            parameters:
              varStr: $(testVariable)