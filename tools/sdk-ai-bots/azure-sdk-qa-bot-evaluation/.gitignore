# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/

# Runtime evaluation artifacts (per-run outputs, not source).
cache/
output/
results/.log/
*-eval-results.json
evaluate-result-*

# Dataset-preparation working dirs (blob downloads / conversions are transient).
online-qa-tests/
online-tests/
eval-archive/
smoke-tests/

# Local secrets / env.
.env
auth.json
