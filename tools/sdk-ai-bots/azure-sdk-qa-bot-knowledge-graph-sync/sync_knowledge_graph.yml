name: Sync Knowledge Graph

trigger: none
pr: none

parameters:
  # Manual full rebuild — leave default for the scheduled daily run.
  # Toggle to `true` in the "Run pipeline" dialog to wipe and re-index
  # everything from scratch (expensive — 1–3 hours, large embedding cost).
  - name: fullGraphrag
    displayName: 'Full GraphRAG rebuild (skip incremental)'
    type: boolean
    default: false

schedules:
- cron: "0 3 * * *"  # Daily at 3 AM UTC (1h after the doc sync at 02:00)
  displayName: Daily knowledge graph sync
  branches:
    include:
    - main

resources:
  repositories:
    - repository: 1ESPipelineTemplates
      type: git
      name: 1ESPipelineTemplates/1ESPipelineTemplates
      ref: refs/tags/release
    - repository: azure-sdk-docs-eng.ms
      type: git
      name: internal/azure-sdk-docs-eng.ms
      ref: main
    - repository: internal-wiki
      type: git
      name: internal/internal.wiki
      ref: wikiMaster

extends:
  template: v1/1ES.Official.PipelineTemplate.yml@1ESPipelineTemplates
  parameters:
    settings:
      skipBuildTagsForGitHubPullRequests: true
      networkIsolationPolicy: Permissive
    sdl:
      # Turn off the build warnings caused by disabling some sdl checks
      createAdoIssuesForJustificationsForDisablement: false
      sourceAnalysisPool:
        name: azsdk-pool
        image: windows-2022
        os: windows
      eslint:
        enabled: false
        justificationForDisabling: 'ESLint injected task has failures because it uses an old version of mkdirp. We should not fail for tools not controlled by the repo. See: https://dev.azure.com/azure-sdk/internal/_build/results?buildId=3499746'
      codeql:
        compiled:
          enabled: false
          justificationForDisabling: CodeQL times our pipelines out by running for 2+ hours before being force canceled.
      componentgovernance:
        enabled: false
        justificationForDisabling: Manually enabling only on the main build job instead of running it on every job.
      psscriptanalyzer:
        compiled: true
        break: true
      policy: M365
      sourceRepositoriesToScan:
        exclude:
          - repository: azure-sdk-docs-eng.ms
          - repository: internal-wiki
    stages:
      - stage: SyncKnowledgeGraph
        displayName: 'Sync Knowledge Graph'
        jobs:
          - job: BuildAndSync
            displayName: 'Build and Execute Knowledge Graph Sync'
            # GraphRAG indexing can take significantly longer than a plain
            # doc sync — bump the per-job timeout well above the default 60m.
            timeoutInMinutes: 240

            variables:
              - group: Release Secrets for GitHub
              - template: /eng/pipelines/templates/variables/image.yml@self
              - template: /eng/pipelines/templates/variables/globals.yml@self
              - name: workingDirectory
                value: '$(Agent.BuildDirectory)/s/azure-sdk-tools/tools/sdk-ai-bots/azure-sdk-qa-bot-knowledge-graph-sync'

            pool:
              name: $(LINUXPOOL)
              image: $(LINUXVMIMAGE)
              os: linux

            steps:
              - checkout: self
                displayName: 'Checkout current repository'

              - checkout: azure-sdk-docs-eng.ms
                displayName: 'Checkout azure-sdk-docs-eng.ms repository'

              - checkout: internal-wiki
                displayName: 'Checkout internal wiki repository'

              - template: /eng/common/pipelines/templates/steps/login-to-github.yml@self
                parameters:
                  ScriptDirectory: $(Agent.BuildDirectory)/s/azure-sdk-tools/eng/common/scripts

              - task: AzureCLI@2
                displayName: 'Resolve federated credentials'
                inputs:
                  azureSubscription: $(SERVICE_CONNECTION)
                  addSpnToEnvironment: true
                  scriptType: bash
                  scriptLocation: inlineScript
                  inlineScript: |
                    echo "##vso[task.setvariable variable=AZURE_CLIENT_ID]$servicePrincipalId"
                    echo "##vso[task.setvariable variable=AZURE_TENANT_ID]$tenantId"
                    echo "##vso[task.setvariable variable=AZURE_SUBSCRIPTION_ID]$(az account show --query id -o tsv)"
                    echo "$idToken" > "$(Agent.BuildDirectory)/id_token"
                    echo "##vso[task.setvariable variable=AZURE_FEDERATED_TOKEN_FILE]$(Agent.BuildDirectory)/id_token"

              - task: UsePythonVersion@0
                displayName: 'Use Python 3.12'
                inputs:
                  versionSpec: '3.12'

              - script: |
                  python --version
                  pip --version
                displayName: 'Show Python version'

              # 1ES agents don't have outbound access to public PyPI; route
              # pip through the internal mirror feed. PipAuthenticate sets
              # PIP_INDEX_URL for the rest of the job.
              - task: PipAuthenticate@1
                displayName: 'Pip Authenticate to internal feed'
                inputs:
                  artifactFeeds: 'public/azure-sdk-for-python'
                  onlyAddExtraIndex: false

              - script: |
                  pip install -e .
                displayName: 'Install Python dependencies'
                workingDirectory: $(workingDirectory)

              - script: |
                  ARGS=""
                  if [ "$FULL_GRAPHRAG" = "True" ] || [ "$FULL_GRAPHRAG" = "true" ]; then
                    ARGS="--full-graphrag"
                  fi
                  echo "Running: sync-knowledge-graph $ARGS"
                  sync-knowledge-graph $ARGS
                displayName: 'Execute knowledge graph sync'
                workingDirectory: $(workingDirectory)
                env:
                  # Pipeline parameter surfaced as env var for the bash wrapper.
                  FULL_GRAPHRAG: ${{ parameters.fullGraphrag }}

                  # Azure App Configuration — everything else is sourced from here.
                  AZURE_APPCONFIG_ENDPOINT: $(AZURE_APPCONFIG_ENDPOINT)

                  # Local clones of the internal repos (checked out above).
                  # The sync project's knowledge-config.json references these
                  # via `localPathEnv`.
                  AZURE_SDK_DOCS_PATH: "$(Agent.BuildDirectory)/s/azure-sdk-docs-eng.ms"
                  AZURE_SDK_WIKI_PATH: "$(Agent.BuildDirectory)/s/internal.wiki"

                  # GitHub App token for private repo access.
                  AZURE_SDK_GITHUB_PAT: $(GH_TOKEN)

                  # Bot reload notification (best-effort; if either is empty
                  # the post-publish notify step is skipped with a warning
                  # and the bot picks up the new snapshot on its next reload).
                  BOT_AGENT_RELOAD_URL: $(BOT_AGENT_RELOAD_URL)
                  BOT_AGENT_ADMIN_TOKEN: $(BOT_AGENT_ADMIN_TOKEN)

                  # Set working directory for temporary files.
                  TMPDIR: $(Agent.TempDirectory)
                  HOME: $(Agent.HomeDirectory)
