parameters:
  - name: sdkPR
    type: object
steps:
  - bash: |
      if [ 2 -lt ${{ #parameters.sdkPR.prLink }} ];then
        echo "2 -lt ${{ parameters.sdkPR.prLink }}"
      if
    displayName: Check parameters

