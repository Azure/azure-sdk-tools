id: skill-authoring-basic-001
name: Basic Skill Authoring Guidance
description: Basic skill authoring guidance task
tags:
  - basic
inputs:
  prompt: |
    I want to create a new skill for API documentation review. What should I include in the SKILL.md file?
expected:
  should_trigger: true
graders:
  - type: text
    name: contains-keywords
    config:
      contains:
        - frontmatter
        - required fields
        - guidelines
